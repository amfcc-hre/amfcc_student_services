# Student Services scope correction

Upload every file in this folder to the existing Student Services repository root.

1. Replace `index.html`, `sw.js` and `admin_index.html`.
2. Add or replace `admin_mode_patch.js`.
3. Open `DELETE_KITCHEN_AND_CLINIC_FILES.txt` and delete each listed file from the repository.
4. Reload Student Services in a private browser window.

The landing page should show Personal Gate Passes only. Kitchen meal check-in and Clinic staff tools now live in the protected Department Operations repository.
