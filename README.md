# Student Services meal check-in correction

Upload every file in this folder to the existing Student Services repository root.

1. Replace `index.html`, `sw.js` and `admin_index.html`.
2. Add or replace `admin_mode_patch.js`.
3. Add or replace `meal_index.html`, `meal_meal.css` and `meal_meal.js`.
4. Open `DELETE_CLINIC_STAFF_FILES.txt` and delete only the three listed Clinic files from the repository.
5. Keep the Student Meal Check-In files in this repository.
6. Reload Student Services in a private browser window.

The landing page should show Student Meal Check-In and Personal Gate Passes. Kitchen staff operations and Clinic staff tools remain in protected Department Operations workspaces.
